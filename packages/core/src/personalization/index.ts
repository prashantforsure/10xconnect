// Contact personalization (Phase 5) — the variable registry, the resolver, and
// the prompt-version hash. Pure + shared by the composer palette, the per-prospect
// preview, and the engine dispatch (cached output reuse).
export * from "./registry";
export * from "./resolver";
export * from "./version";
