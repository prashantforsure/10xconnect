// @10xconnect/adapters — ChannelAdapter implementations.
// This is the ONLY package allowed to import provider SDKs (Unipile, ESP, LLM, TTS)
// per CLAUDE.md §4. A mock adapter and the Unipile adapter arrive in Phase 2.
// Intentionally empty in Step 1.
export {};
